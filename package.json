{
  "name": "prince-wine-store",
  "version": "1.0.0",
  "description": "prince-wine-store website",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel src/index.html --open",
    "build": "parcel build src/index.html"
  },
  "author": "alexey",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.10.2",
    "parcel": "^2.10.2",
    "sass": "^1.69.5"
  },
  "dependencies": {
    "bootstrap": "^5.3.1"
  }
}
